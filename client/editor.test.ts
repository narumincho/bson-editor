import { assertEquals } from "@std/assert";

// 論理構造ではなく表示で左右に配置する場合もあるのでその場合移動先が変化するので注意
Deno.test("editor move down", () => {
  assertEquals(true, true);
  // assertEquals(bsonObject, ejsonAsObject);
});
